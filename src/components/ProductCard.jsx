const ProductCard = ({
        productId,
        productName,
        image,
        description,
        quantity,
        price,
        discount,
        specialPrice,
        about = false,
}) => {
  return (
    <div className="product-card"> 
    </div>
)
}
export default ProductCard;